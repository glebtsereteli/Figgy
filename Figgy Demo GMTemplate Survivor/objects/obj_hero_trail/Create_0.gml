// Set depth to a high negative number
// to make sure it's drawn on top of
// other things.
depth = -3000;

// Set scale.
image_xscale = CFG.Hero.Trail.Scale; // @Figgy: Use config.
image_yscale = image_xscale;